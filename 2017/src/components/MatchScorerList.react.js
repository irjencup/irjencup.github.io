import React, {PropTypes} from 'react';

export default class MatchScorerList extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (<div>MyComponent</div>);
  }
}

MatchScorerList.propTypes = {
};
